name: clean_architecture_lint
description: Custom lints rules used in Clean Architecture.
homepage: https://github.com/ho-doan/clean_architecture
repository: https://github.com/ho-doan/clean_architecture
version: 0.0.7
environment:
  sdk: '>=2.17.0 <4.0.0'

platforms:
  android:
  ios:
  linux:
  macos:
  web:
  windows:

dependencies:
  analyzer: ^5.7.1
  custom_lint_builder: ^0.3.2
  # analyzer: ^5.13.0
  # analyzer_plugin: ^0.11.2
  # analyzer: ^6.0.0
  # analyzer_plugin: ^0.11.3
  # custom_lint_builder: ^0.5.1
dev_dependencies:
  cli_util: ^0.3.5
  # custom_lint: ^0.5.1
  custom_lint: ^0.3.2
  lints: ^2.1.1
